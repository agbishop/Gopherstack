/.idea
/Gopherstack.iml
# JetBrains Safe Write and editor temporary/swap files
.!*
*.!*
*.swp
*.swo
*.swn
.*.swp
.*.swo
.*.swn
*~
\#*#
/PLAN.md
/new.sh
/**/*.out
/awsgs
*.out
main 
**/main
/gopherstack
test/terraform/gopherstack
test/terraform/bin/
/bin/
.DS_Store
coverage.html
**/*.txt
*.txt
!test/e2e/crosssdk/requirements.txt
**/*.log
*.log
*.pdf
test/e2e/failures/go.mod.bak
/go.mod.bak
*.png
!dashboard/static/favicon.png
*.tmp
copilot_changes.patch
ui/node_modules/
ui/.svelte-kit/
# Stray generated dir left behind when svelte-kit is run from the repo root
# instead of ui/. Ignored so it can't be committed by accident.
/.svelte-kit/
ui/build/
/dashboard/static/spa/*
!/dashboard/static/spa/.keep
.beads/*
!.beads/issues.jsonl
.gemini/
.runtime/
CLAUDE.md

# Gas Town (added by gt)
.claude/*
!.claude/skills/
.logs/
__pycache__/
state.json
CLAUDE.local.md
e2e.test

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
*.py
!test/e2e/crosssdk/*.py
node_modules/
.terraform
*.tfstate
*.tfstate.backup
lambda.zip
.terraform.lock.hcl
.terraform.lock.hcl
/releaser

# personal local Claude Code settings (never sync bypass perms to origin)
.claude/settings.local.json

# session resume checkpoint (transient)
checkpoint.md

# PGO: intermediate pprof capture from scripts/pgo.sh (renamed to
# default.pgo on success, which IS committed — do not ignore that file)
/cpu.pprof

# gendocs binary if built at the repo root (go build ./cmd/gendocs)
/gendocs
/test/terraform/bin/gopherstack
/examples/s3-lambda-processor/bootstrap

# Playwright MCP session artifacts (console logs, traces)
.playwright-mcp/

# Beads / Dolt files (added by bd init)
.beads/proxieddb/
*.gate.lock*
